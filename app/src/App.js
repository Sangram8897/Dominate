import React from 'react';
import {View, Text} from 'react-native';
import Auth from 'components/auth';

const App = () => {
  return (
    <View style={{flex:1}}>
      <Auth />
    </View>
  );
};

export default App;
